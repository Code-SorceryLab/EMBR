"""Paper assets from evaluation runs: figures, tables, the results page, the demo pages, the
release manifest. Every builder reads a run directory or recomputes from the harness; none
of them takes a number from prose."""
